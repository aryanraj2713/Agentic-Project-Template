"""Backend application package for the FastAPI service.

All backend source modules (main, config, db, models, schemas, routes, health)
live within this single ``app`` package, one directory level below ``apps/api``.
"""
